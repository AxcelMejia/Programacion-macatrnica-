#include <stdio.h>
/* incremento de precio.
El programa, al recibir como dato el precio de un producto importado, incrementa 11% el mismo si esta es inferior a $1,500.
PRE y NPR: variable de tipo real*/

void main(void)
{
    float PRE, NPR;
    printf("infrese el precio del producto: ");
    scanf("%f", &PRE);
}
